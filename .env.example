NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PASSWORD=change-me
WATCH_TOKEN=change-me-too
# Set to exactly "true" to drop the admin password on the catalog and on
# round creation. Anything else (including absent) keeps the password required.
# Delete this variable to re-enable the password.
ADMIN_OPEN=
